{
  "name": "discord-codex-bot",
  "version": "0.2.0",
  "description": "Discord bot that bridges thread conversations to OpenAI Codex CLI via codex exec — supports session resume, role-contract dispatch (worker/verifier/reviewer/synthesizer), and sandbox isolation per Discord thread.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/fredchu/discord-codex-bot.git"
  },
  "scripts": {
    "start": "node --env-file=.env --import=tsx src/index.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "discord.js": "^14.22.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "tsx": "^4.19.0",
    "typescript": "^5.9.2"
  }
}
